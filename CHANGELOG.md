# Pulse 更新日志

所有对外可见的改动都记录在此（新→旧）。每轮改动完成后即时推送。

## 2026-09-06 · 深夜（`42f8f61`）

- **新功能：投票截止时间**——发布投票时可选截止时刻，到期后投票页拦截并提示；后台标题旁倒计时胶囊（最后 1 小时变红加急）；列表按「即将截止」前置（迁移 0013）
- **新功能：PWA 离线**——Service Worker 注册（静态资源 cache-first、页面与 API network-first 回退缓存），断网可看已加载页面；配合移除字体 CDN 外链（改系统字体栈），离线观感完整
- **新功能：美化系统 + ✨ 开关**——右下角 ✨ 浮钮即点即换：总开关 + 五档预设（第一版全量 / 二版 1·3·5 / 三版 1·4·5 / 五版 1·5 / 六版全量+丝滑），HTML 属性早期生效不闪变；六项美化：①多色彩带 ②空状态插画 ③截止倒计时 ④全站玻璃质感 ⑤丝滑动效（含折射玻璃 6）
- **新功能：创建时即可选「结果公示」**——发布抽签表单新增分段选择（盲选默认/公示结果），不必创建后再到后台切换
- **修复：公示抽签访客无法参与（P1）**——参与区显隐、canDraw、姓名登记三处都按盲选态写死，公示态（blind=false）整个参与入口被结果墙顶掉；三处对公示进行中放行，已抽过者收起舞台只看结果墙
- **修复：公示抽签抽完后计数冻结**——公示态 wallRevealed 初始即 true，抽完再 set 不触发复位 effect，performing 永久挂起、页面冻结在演出前快照；新增 drewTick 强制触发复位
- **修复：后台分段切换滑块动画失效**——SegmentToggle 的 `.seg-mover` 类名与抽签页揭晓切换撞车，被 styles.css 末尾同名规则覆盖；改独立类名 `.segment-toggle-mover`（三组均实测 CSSTransition 弹性曲线 + 终态对齐）
- **修复：poll-repo 集成测试挂 14 例**——seed:v1 防复活机制合入后测试夹具没建 `_migrations` 表（本地 Node 20 跑不了 npm test 一直没暴露）；夹具补建表，43/43 复绿

## 2026-09-06 · 晚二（`faacb66`）

- **新增玻璃方案对照页 `/compare`**：磨砂（现行）/ SVG 位移折射（shuding 改造）/ 纯透明基线，同屏对比；折射板可拖拽
- **折射板修复**：挂载时序死锁（滤镜与折射层互等）+ 拖拽指针捕获（出界不丢）+ 鼠标液态隆起（悬停时局部向光标弯折）
- **结论**：折射观感最接近 Apple 发布会，但 Chromium 专属、开销高——适合盲选卡等强调元素；全站维持 CSS 磨砂

## 2026-09-06 · 晚（`2570dbc`）

- **后台一致性修复**：抽签创建表单补上玻璃面板（此前投票侧有框、抽签侧没有的不一致）
- **按钮调和落地**：登录按钮放大（h-10/48，字 15→17px）、主题钮改紧凑方形 38/44/48
- **玻璃面板内按钮按压缩放反馈** + **表单子项错峰入场动效**
- CF 隧道信任改通配 `*.trycloudflare.com`（隧道重连换地址后登录不再失效）

## 2026-09-06 · 下午（`06c946a`…最新）

- **新功能：标题备注行**——发布投票/抽签时可填可选备注（≤120 字），展示在标题下方小字；不填则与旧版一致（迁移 0011）
- **新功能：抽签导航对齐投票逻辑**——「抽签」点进去直接是当前抽签参与页；历史列表移至 `/draw/history`
- **新功能：管理员删除历史**——投票/抽签历史与后台列表的行右侧叉号删除（发起人本人；无主历史登录者可清理），级联删除
- **新功能：抽签结果公示开关**——发起人后台「公开结果/隐藏结果」一键切换；公示后所有访客可见抽取情况与「公示名单」（谁抽中了什么）
- **新功能：记名投票「参与明细」**——发起人后台可见谁、何时、选了什么
- **修复：登录会话无法保持**——Better Auth cookieCache 与响应桥接冲突导致主凭证 cookie 被吞（P1 根因）；关闭 cookieCache 修复
- **修复：隧道/预览信任来源**——支持 `BETTER_AUTH_EXTRA_ORIGINS` / `VITE_BETTER_AUTH_EXTRA_ORIGINS` 通配信任

## 2026-09-06 · 下午（`e556ae2`…`fcd118b`）

- **修复：毛玻璃失效（构建链根因）**——Tailwind v4 的 Lightning CSS 前缀合并会丢弃写在 `-webkit-` 之前的标准 `backdrop-filter`，导致部分浏览器只剩透明底色；声明顺序交换 + 声明 browserslist 现代目标，产物双声明齐全
- **新功能：后台页面液态玻璃化**——创建投票/抽签/登录表单包进玻璃面板；后台列表行淡入上移错峰动效；全站按钮「推拉」按压手感
- **桌面端加宽**——内容列与头部：手机不变，平板 672px / 桌面 768px（响应式断点，非缩放）
- **修复：头部脚本加载前的中间状态**——主题按钮 SSR 直接渲染纯图标、导航滑块定位前隐藏

## 2026-09-06 · 中午（`13c6545`）

- **修复：九宫格两个叠加 bug**——①中奖判定按签位名比对，同名签位多格齐亮 → 改按格子索引；②跑灯落点数学错误，灯停随机格与结果不符 → 重算落点恒停结果格
- **新功能：标题备注行的前身（该轮为数据层准备，完整功能见上）**

## 2026-09-06 · 中午（`b638c49`、`c53f68c`）

- **桌面端内容列加宽**——手机不变，平板 672px / 桌面 768px 响应式断点
- **全站按钮「推拉」按压手感**
- **CSS 全量审计**——21 项美化签名核对，除 backdrop-filter 外无其他丢失

## 2026-09-06 · 上午（`8082eab`、`8231323`）

- **修复：九宫格两个叠加 bug**——中奖判定按签位名比对导致同名格齐亮；跑灯落点数学错误导致灯停随机格与结果不符（两修叠加复现自用户实测）

## 2026-09-06 · 上午（`4cb8b00` 大版本修复）

- **毛玻璃失效根修**（构建链丢弃标准 backdrop-filter 的首发修复，后续 `e556ae2` 根因定位更深）
- **后台页面液态玻璃化 + 入场动效**
- 登录会话、删除权限守卫、种子幂等等数据层修复
- 自托管 Inter 字体，移除字体 CDN 外链

## 2026-09-06 · 凌晨（`59de1e5` 之前基线）

- 抽签模块、盲选机制、名单核对、邮箱登录、管理面板、三种揭晓动画框架等（见 git log）

## 2026-09-06 · 傍晚（`3b48e44`）

- **导航栏桌面放大**：高度 64→80px，导航字 15→18px、内边距加宽，logo 加大（平板 68px 过渡档）
- **右侧按钮群舒展**：导航与主题/登录间距加大，不再紧凑
- **按钮调和**：登录=突出玻璃药丸（更大更醒目），主题切换=紧凑方形图标钮（常见网站范式）
- **切换动效统一**：登录/注册、投票/抽签滑块改用导航药丸同款 `.42s` 弹性过冲曲线（更灵动）
- 核对：导航滑块动效参数与 liquid-glass-template 逐字一致，从未改动

- 抽签「公开结果」开关的边界统一：无主（建号前）内容登录者可公示

## 2026-09-06 · 晚三（`cd1a1f3`）

- **修复：创建抽签发布必失败（P1）**——结果公示列接入时 insert 列与占位/参数错位（8 列 7 值 6 参），Postgres 拒绝绑定；参数对齐修复。此前所有「新发布抽签」都会触发此错。
- 预览验证：创建「公示结果」抽签 → 详情页立即公示（徽章+后台隐藏开关）；空备注不渲染。
